def versionName = "5.0.2"

task release (type: Zip) {
	from '.'
	include '.htaccess'
	include 'index.php'
	include 'application/**'
	include 'system/**'
	exclude 'application/tests/**'
	archiveName 'rest-server-' + versionName + '.zip'
	destinationDir file('release/')
}